name: IAR Demo - Python Classifier Tests

on:
  push:
    branches: ["main"]
    paths:
      - "iar-demo/api/**"
      - ".github/workflows/iar-demo-python-tests.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "iar-demo/api/**"
      - ".github/workflows/iar-demo-python-tests.yml"

jobs:
  test-python-classifier:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python (with pip cache)
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            iar-demo/api/requirements-dev.txt

      - name: Install test deps
        working-directory: iar-demo
        run: |
          python -m pip install --upgrade pip
          pip install -r api/requirements-dev.txt

      - name: Detect changed files
        id: changes
        shell: bash
        run: |
          set -e

          BEFORE="${{ github.event.before }}"
          AFTER="${{ github.sha }}"

          if [ -z "$BEFORE" ] || echo "$BEFORE" | grep -q '^0\+$'; then
            BEFORE="$(git rev-parse "${AFTER}~1" 2>/dev/null || true)"
          fi

          CHANGED=""
          if [ -n "$BEFORE" ]; then
            CHANGED="$(git diff --name-only "$BEFORE" "$AFTER" || true)"
          fi

          CHANGED_API="$(echo "$CHANGED" | grep '^iar-demo/api/' || true)"

          if echo "$CHANGED_API" | grep -q '^iar-demo/api/classify\.py$'; then
            echo "what=classify.py" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ -n "$CHANGED_API" ]; then
            LIST="$(echo "$CHANGED_API" | sed 's|^iar-demo/api/||' | head -n 3 | paste -sd ', ' -)"
            COUNT="$(echo "$CHANGED_API" | wc -l | tr -d ' ')"
            if [ "$COUNT" -gt 3 ]; then
              echo "what=iar-demo/api: $LIST (+$((COUNT-3)) more)" >> "$GITHUB_OUTPUT"
            else
              echo "what=iar-demo/api: $LIST" >> "$GITHUB_OUTPUT"
            fi
          elif [ -n "$CHANGED" ]; then
            LIST="$(echo "$CHANGED" | head -n 3 | paste -sd ', ' -)"
            COUNT="$(echo "$CHANGED" | wc -l | tr -d ' ')"
            if [ "$COUNT" -gt 3 ]; then
              echo "what=$LIST (+$((COUNT-3)) more)" >> "$GITHUB_OUTPUT"
            else
              echo "what=$LIST" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "what=repo files" >> "$GITHUB_OUTPUT"
          fi

      - name: Run pytest + coverage (verbose + colored)
        working-directory: iar-demo
        env:
          PY_COLORS: "1"
          FORCE_COLOR: "1"
        run: |
          pytest -vv --tb=no --no-summary -r0 --color=yes api/tests \
            --junitxml=pytest-results.xml \
            --cov=api \
            --cov-report=term-missing \
            --cov-report=html:htmlcov \
            --cov-report=xml:coverage.xml

      - name: Upload coverage HTML artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: iar-demo-coverage-html
          path: iar-demo/htmlcov
          retention-days: 7

      - name: Send Telegram notification
        if: always()
        working-directory: iar-demo
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_CLASS_CHAT_ID: ${{ secrets.TELEGRAM_CLASS_CHAT_ID }}

          ACTOR: ${{ github.actor }}
          STATUS: ${{ job.status }}
          SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          REPO: ${{ github.repository }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          WHAT_CHANGED: ${{ steps.changes.outputs.what }}
        shell: bash
        run: |
          python - <<'PY'
          import os
          import re
          import xml.etree.ElementTree as ET
          import urllib.request
          import urllib.parse

          bot = os.environ.get("TELEGRAM_BOT_TOKEN", "").strip()
          chat_dm = os.environ.get("TELEGRAM_CHAT_ID", "").strip()
          chat_class = os.environ.get("TELEGRAM_CLASS_CHAT_ID", "").strip()

          actor = os.environ.get("ACTOR", "someone")
          status = os.environ.get("STATUS", "unknown")
          sha = os.environ.get("SHA", "")[:7]
          run_url = f"https://github.com/{os.environ.get('REPO','')}/actions/runs/{os.environ.get('RUN_ID','')}"
          commit_msg = os.environ.get("COMMIT_MSG", "")
          what_changed = os.environ.get("WHAT_CHANGED", "iar-demo/api/*")

          def send(chat_id: str, text: str):
              url = f"https://api.telegram.org/bot{bot}/sendMessage"
              data = urllib.parse.urlencode({
                  "chat_id": chat_id,
                  "text": text,
                  "disable_web_page_preview": "true",
              }).encode("utf-8")
              req = urllib.request.Request(url, data=data, method="POST")
              with urllib.request.urlopen(req, timeout=20) as resp:
                  resp.read()

          if not bot or not chat_dm:
              print("Telegram not configured (missing TELEGRAM_BOT_TOKEN or TELEGRAM_CHAT_ID).")
              raise SystemExit(0)

          # Coverage %
          coverage_pct = "Unknown"
          try:
              tree = ET.parse("coverage.xml")
              root = tree.getroot()
              line_rate = root.attrib.get("line-rate")
              if line_rate is not None:
                  coverage_pct = f"{round(float(line_rate) * 100)}%"
          except Exception:
              pass

          # Test list from JUnit
          tests_lines = []
          try:
              t = ET.parse("pytest-results.xml")
              r = t.getroot()
              testcase_elems = r.findall(".//testcase")
              for tc in testcase_elems:
                  name = tc.attrib.get("name", "unknown_test")
                  failed = tc.find("failure") is not None or tc.find("error") is not None
                  skipped = tc.find("skipped") is not None
                  icon = "⏭️" if skipped else ("❌" if failed else "✅")
                  tests_lines.append(f"{icon} {name}")
              if not tests_lines:
                  tests_lines = ["(No tests parsed — check Actions log)"]
          except Exception:
              tests_lines = ["(Unable to parse test list — check Actions log)"]

          emoji = "🟢" if status == "success" else "🔴"
          header = "SUCCESS" if status == "success" else status.upper()

          msg = "\n".join([
              f"{emoji} IAR CI: {header}",
              f"{actor} pushed {what_changed}",
              f"Commit: {sha}",
              f"Coverage: {coverage_pct}",
              "",
              "Tests:",
              *tests_lines,
              "",
              f"Run: {run_url}",
          ])

          # ALWAYS DM you
          send(chat_dm, msg)
          print(f"Sent Telegram DM to TELEGRAM_CHAT_ID={chat_dm}")

          # ONLY ALSO send to class group when commit contains EXACT phrase 'class demo'
          # (This will NOT trigger for 'demo' alone.)
          is_class_demo = re.search(r"\bclass demo\b", commit_msg, re.IGNORECASE) is not None
          if is_class_demo and chat_class:
              send(chat_class, msg)
              print(f"Sent Telegram group message to TELEGRAM_CLASS_CHAT_ID={chat_class}")
          else:
              print("Not a class demo commit (or TELEGRAM_CLASS_CHAT_ID not set).")
          PY